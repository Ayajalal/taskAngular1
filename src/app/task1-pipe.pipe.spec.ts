import { Task1PipePipe } from './task1-pipe.pipe';

describe('Task1PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Task1PipePipe();
    expect(pipe).toBeTruthy();
  });
});
